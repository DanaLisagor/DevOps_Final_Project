apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-flask-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hello-flask
  template:
    metadata:
      labels:
        app: hello-flask
    spec:
      containers:
      - name: hello-flask-container
        image: danalisagor/hello-flask:latest
        ports:
        - containerPort: 5000
        resources:
          requests:
            cpu: 100m
        env:
        - name: PORT
          valueFrom:
            configMapKeyRef:
              name: hello-flask-config
              key: PORT
        - name: SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: hello-flask-secret
              key: SECRET_KEY
        livenessProbe:
          httpGet:
            path: /
            port: 5000
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 5000
          initialDelaySeconds: 5
          periodSeconds: 5
